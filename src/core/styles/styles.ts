import * as css from './styles.module.css'
import * as link from './link.module.css'

export const styles = {
  outline: css.outline,
  boxShadow: {
    paper: css.paperShadow,
  },
  link: {
    primary: link.primary,
    neutral: link.neutral,
    small: link.small,
    large: link.large,
  },
}
